/**
 * Stub after Mongo→MySQL migration.
 * Prefer live Tally sync via desktop-agent instead of seeding fake vouchers.
 */
console.log('create-demo-data.js: skipped — use desktop-agent sync against MySQL backend.');
console.log('Admin seed: npm run create:single-admin');
process.exit(0);
